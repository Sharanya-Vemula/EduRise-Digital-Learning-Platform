<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/teacher_fragment_bg">

    <!-- Loading Indicator -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        android:indeterminateTint="@color/teacher_button" />

    <!-- No Announcements -->
    <TextView
        android:id="@+id/tvNoAnnouncements"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No announcements"
        android:textSize="16sp"
        android:textColor="@color/teacher_text_secondary"
        android:gravity="center"
        android:visibility="gone"
        android:padding="16dp" />

    <!-- Announcements RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvAnnouncements"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="8dp"
        android:scrollbars="vertical" />

</LinearLayout>
